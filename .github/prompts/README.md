This folder contains small, deterministic prompts used by phase agents for focused OpenAPI operations.

Use each prompt as a reentrant instruction set producing `diff`-style YAML output when possible.

> **Note:** the entire `.github` collection forms a continuous, agent‑driven workflow. The orchestrator automatically cycles through prompts/agents without pauses or manual intervention; no step should "stop" the chain.
